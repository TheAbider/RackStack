# Credentials (org-specific URLs, keys, passwords)
# Base + company config under both naming schemes; only the example is tracked
rackstack.config.json
*.rackstack.config.json
defaults.json
*.defaults.json
!rackstack.config.example.json
!defaults.example.json

# Build artifacts (monolithic, exe, archives)
builds/
*.exe
RackStack v*.ps1

# Local development files (not for public repo)
local/

# Wiki (separate git repo, lives at wiki/)
wiki/

# Transcripts and logs
*.transcript
*.log

# IDE and editor files
.vscode/
*.code-workspace
.idea/

# OS files
Thumbs.db
Desktop.ini

# Batch config exports
batch_config*.json

# Environment files
.env*

# Temporary files
*.tmp
*.bak
*.swp
*~

# Pester NUnit output + JaCoCo coverage (per-run, not source)
Tests/pester-results.xml
Tests/coverage.xml
